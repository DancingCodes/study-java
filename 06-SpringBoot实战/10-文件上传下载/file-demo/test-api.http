### 测试文件上传下载

### 1. 上传单个文件（用 IDEA 的 HTTP Client 无法直接上传文件，用 curl 或 Postman 测试）
### curl -F "file=@C:/path/to/your/file.txt" http://localhost:8080/files/upload
POST http://localhost:8080/files/upload
Content-Type: multipart/form-data; boundary=boundary

--boundary
Content-Disposition: form-data; name="file"; filename="test.txt"
Content-Type: text/plain

Hello, this is a test file for upload demo.
--boundary--

### 2. 查看文件列表
GET http://localhost:8080/files

### 3. 下载文件（把 xxx 替换成上传后返回的文件名）
GET http://localhost:8080/files/download/xxx.txt

### 4. 删除文件（把 xxx 替换成上传后返回的文件名）
DELETE http://localhost:8080/files/xxx.txt
