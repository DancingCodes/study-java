# 练习 2：负载均衡
# 将请求轮询分发到 8080 和 8081 两个 Spring Boot 实例
#
# 使用方法：
#   1. 启动两个 Spring Boot 实例：
#      java -jar app.jar --server.port=8080
#      java -jar app.jar --server.port=8081
#   2. 把这个文件的内容复制到 nginx.conf 的 http 块中
#   3. nginx.exe -s reload
#   4. 多次访问 http://localhost，观察请求被分发到不同实例

# TODO: 定义 upstream backend，包含两个后端服务
# upstream backend {
#     server localhost:8080;
#     server localhost:8081;
# }

server {
    listen 80;
    server_name localhost;

    location / {
        # TODO: 将请求代理到 upstream backend
    }
}
