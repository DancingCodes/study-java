# 练习 1：反向代理
# 将 80 端口的请求代理到本机 8080 端口的 Spring Boot 应用
#
# 使用方法：
#   1. 把这个文件的内容复制到 nginx/conf/nginx.conf 的 http 块中
#      （替换掉原有的 server 块）
#   2. nginx.exe -t 检查配置
#   3. nginx.exe -s reload 重新加载
#   4. 先启动 Spring Boot 应用（8080 端口）
#   5. 浏览器访问 http://localhost，请求会被转发到 8080

server {
    listen 80;
    server_name localhost;

    # TODO: 将所有请求代理到 http://localhost:8080
    # 提示：
    #   proxy_pass http://localhost:8080;
    #   proxy_set_header Host $host;
    #   proxy_set_header X-Real-IP $remote_addr;
    #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    location / {
        # TODO: 在这里写代码
    }
}
